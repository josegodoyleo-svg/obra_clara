{
  "name": "obraclara-ai",
  "version": "0.1.0",
  "private": true,
  "description": "ObraClara AI — Monitoreo inteligente de contratos de obras públicas en Bolivia",
  "workspaces": [
    "contract",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "compile": "npm run compile --workspace=contract",
    "deploy": "npm run deploy --workspace=contract",
    "test:contract": "npm run test --workspace=contract"
  },
  "devDependencies": {
    "pdfkit": "^0.20.2"
  }
}
