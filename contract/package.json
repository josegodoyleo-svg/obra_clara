{
  "name": "@obraclara/contract",
  "version": "0.1.0",
  "private": true,
  "description": "Contrato inteligente ObraClara — Registro de hashes y gestión de obras en HSK Chain",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network hskTestnet",
    "node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
